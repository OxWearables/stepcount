name: cwa

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:

  prepare-artifacts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.8"
          cache: 'pip'
          cache-dependency-path: setup.py
      - name: Install dependencies
        run: |
          set -e
          python -m pip install --upgrade pip
          python -m pip install -e .  # install as editable to locate the torch hub cache more easily

      # Perform a run to cache the torch hub repo and download the model files (SSL)
      - name: Smoke test
        run: stepcount tests/data/nano-sample.csv.gz

      # Perform a run to download the model files (RF)
      - name: Smoke test (RF)
        run: stepcount tests/data/nano-sample.csv.gz -t rf

      - name: Upload model joblib files
        uses: actions/upload-artifact@v4
        with:
          name: model-files
          path: src/stepcount/*.joblib.lzma

      - name: Upload torch_hub_cache
        uses: actions/upload-artifact@v4
        with:
          name: torch_hub_cache
          path: src/stepcount/torch_hub_cache

  cwa:
    needs: prepare-artifacts
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.8", "3.9", "3.10"]

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
          cache-dependency-path: setup.py

      - name: Install dependencies
        run: |
          set -e
          python -m pip install --upgrade pip
          python -m pip install -e . "pytest>=7.0"

      - name: Download model files
        uses: actions/download-artifact@v4
        with:
          name: model-files
          path: src/stepcount

      - name: Download torch_hub_cache
        uses: actions/download-artifact@v4
        with:
          name: torch_hub_cache
          path: src/stepcount/torch_hub_cache

      - name: Smoke test, nano-sample.csv.gz
        run: stepcount tests/data/nano-sample.csv.gz

      - name: Smoke test, nano-sample.csv.gz (RF)
        run: stepcount tests/data/nano-sample.csv.gz -t rf

      - name: Numerical regression, tiny-sample.cwa.gz
        id: regression
        env:
          STEPCOUNT_RUN_MODEL_REGRESSION: "1"
          STEPCOUNT_REGRESSION_OUTPUT_DIR: regression-outputs
        run: python -m pytest tests/test_cwa_regression.py

      - name: Upload numerical regression outputs
        if: always() && steps.regression.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: numerical-regression-${{ matrix.os }}-${{ matrix.python-version }}
          path: regression-outputs
